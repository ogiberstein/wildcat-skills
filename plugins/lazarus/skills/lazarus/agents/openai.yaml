interface:
  display_name: "Lazarus"
  short_description: "Proof-checked historical Ethereum fixtures with exact-request replay and no live fallback."
  default_prompt: "Use Lazarus to capture, verify or replay a finite historical Ethereum fixture without a live provider fallback."
