interface:
  display_name: "Alexandria"
  short_description: "Digest-bound lending-data releases with reviewed credit views and address queries."
  default_prompt: "Use Alexandria to ingest or verify a lending-data release, derive its reviewed credit view, or query a verified address index without hiding coverage gaps."
