Runbook: rolling Fiat frontiers

Step 1: Publish the rolling marketplace frontier contract

- **Goal.** Add one enforced next-job command to every plugin landing page and
reconcile the marketplace's copied frontier prose in one atomic change.

- **Entry.** `origin/main` at `6d751b3`, with issue 63 open and the worktree
clean apart from `.hexaemeron/` controller state.

- **Exit.** Nine plugin landing READMEs contain one unique rolling job field
with the canonical invocation and exact cold-read suffix; Hermes has a short
landing page; current frontiers agree across landing pages, the root selection
table and every mutable marketplace context copy; Fiat's browsing README is
removed with every other first-party shadow skill README; stale issue-phase and
Euler prose is corrected; the study and runbook are committed under `docs/`;
and the complete
repository, Foundry, prose and protected-file checks pass.

- **Files.** Create `plugins/hermes/README.md`,
`docs/rolling-fiat-frontiers-study.md` and
`docs/rolling-fiat-frontiers-runbook.md`. Change the other eight
`plugins/*/README.md` landing pages, `README.md`, mutable first-party files
whose `marketplace-context` frontier is copied, Fiat's canonical browsing
contracts and shadow README references, authoritative prose templates or
renderers when needed, generated
outputs when their sources change, and `tests/test_marketplace_prose.py`.

- **Tests.** Extend `tests/test_marketplace_prose.py` to require exactly nine
landing pages, one canonical command per landing, unique topics, the exact
common suffix, no command outside landing pages, exact frontier agreement and
the absence of first-party shadow skill READMEs. Run every Python command in the repository
`AGENTS.md`, Pandects `forge build` and `forge test`, frontmatter and link
checks covered by the root suite, Proscribed and Imprimatur over changed
first-party prose, protected-file comparisons, `git diff --check`, and the
root suite again from the committed tree.

The demonstration path is the new marketplace-prose test failing on a removed,
duplicated or divergent next-job field and passing on all nine completed
landing pages. The resulting pull request is the single review boundary.
